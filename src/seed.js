/** Optional: no default site seed — sites are created per user after Google sign-in. */

export async function seedIfEmpty() {
  // Intentionally empty; auth + user-scoped sites replace global demo data.
}
